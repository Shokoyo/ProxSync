/**
 * Created by Jeremias on 23.09.2017.
 */
function removeFromFavorites(key) {
    /*var oldKey = key;
    key = key.split(".").join("-");
    if(key !== "") {
        firebase.database().ref("/users/" + uid + "/favorites/" + key).remove();
        document.getElementById("card-" + oldKey).style.display = 'none';
    } else {
        console.log("corrupted favorite entry");
    }*/
}